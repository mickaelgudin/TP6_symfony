{% extends 'base.html.twig' %}

{% block title %}ElementaryType{% endblock %}

{% block body %}
    <h1>ElementaryType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ elementary_type.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ elementary_type.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('elementary_type_index') }}">back to list</a>

    <a href="{{ path('elementary_type_edit', {'id': elementary_type.id}) }}">edit</a>

    {{ include('elementary_type/_delete_form.html.twig') }}
{% endblock %}
