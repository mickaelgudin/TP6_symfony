<form method="post" action="{{ path('pokemon_delete', {'idp': pokemon.idp}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pokemon.idp) }}">
    <button class="btn">Delete</button>
</form>
